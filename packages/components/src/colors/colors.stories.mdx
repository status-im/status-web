import { Meta, ColorPalette, ColorItem } from '@storybook/blocks'
import * as colors from '@felicio/colors'

<Meta title="Colors/Overview" />

<ColorPalette>
  {Object.keys(colors).map(key => (
    <ColorItem
      title={key}
      colors={colors[key]}
      style={{ whiteSpace: 'nowrap' }}
    />
  ))}
</ColorPalette>
